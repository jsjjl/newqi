const https = require('https');
const fs = require('fs');

// We need a token. We can grep it or just assume we don't have one here and see if it requires auth.
// Actually, I can't easily get the token.
